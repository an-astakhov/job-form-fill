# Job Form Fill

Personal-use Chrome extension for AI-assisted autofilling of job application forms.

## Current Status

Step 1 is scaffolded:

- Manifest V3 configuration
- TypeScript + Vite project setup
- Popup UI skeleton

## Planned Steps

See [docs/project-spec.md](/c:/Github/job-form-fill/docs/project-spec.md).

## Intended Commands

```powershell
npm install
npm run build
```

Then load the generated `dist/` folder as an unpacked extension in Chrome.
